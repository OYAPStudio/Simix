/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  images: {
    unoptimized: true,
  },
  // ✅ removed experimental.appDir — no longer needed
}

module.exports = nextConfig
